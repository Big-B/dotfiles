# Aliases file
# Example aliases
# alias zshconfig="mate ~/.zshrc"
alias bc="bc -l" # Use decimals
alias egrep="egrep --color"
# No correct
alias git="nocorrect git"

alias c="clear"

alias psall="ps -eLo pid,tid,psr,rtprio,policy,pri,state,%cpu,cputime,user,stat,wchan:20,comm:32"
